import styled from 'styled-components';

export const Link = styled.a`
  color: #1451ff;
  font-weight: 500;
  cursor: pointer;
  transition: 0.3s;
  cursor: pointer;

  &:hover {
    opacity: 0.8;
  }
`;
